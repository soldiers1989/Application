package com.chad.learning.lambda;

public class Notes {
    /**
     * 语法一：()->{} 其中()和{}可以看情况去掉，()只有一个参数可去掉，{}里逻辑只有一行可去掉
     *
     * 语法二：::（两个英文冒号） ::是对语法一的简化
     *         静态方法引用：ClassName::staticMethodName
     *         实例上的实例方法引用：instanceReference::methodName
     *         构造方法引用：ClassName::new
     */
}
